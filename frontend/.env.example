# Example frontend environment variables (do not put secrets here)
# Copy to .env and fill values locally. Keys must be prefixed with VITE_

VITE_BACKEND_URL="http://localhost:4000"
VITE_RAZORPAY_KEY_ID=""
